# Go Tweet Yourself

To run, setup a `creds.json` file with

```
{
    "consumer_secret": "bla",
    "consumer_token": "foo",
    "access_token": "buzz",
    "secret_token": "zap"
}
```

Then run...

```
go run twitter.go -user=jwpe
```

or
```
./twitter -user=joshfinnie
```